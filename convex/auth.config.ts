// https://needed-crab-27.clerk.accounts.dev

const authConfig = {
    providers: [
        {
            domain: "https://needed-crab-27.clerk.accounts.dev",
            applicationID: "convex"
        }
    ]
}

export default authConfig